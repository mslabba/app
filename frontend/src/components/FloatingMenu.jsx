/**
 * @deprecated Replaced by AppShell sidebar navigation (Workstream B2).
 * Kept as a no-op so any residual imports do not render the rainbow FAB.
 */
const FloatingMenu = () => null;

export default FloatingMenu;
